spring:
  datasorurce:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:8080/practice1
    username: postgres
    password: root
  jpa:
    hibernate:
    ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibetnate: 
        format_sql: true